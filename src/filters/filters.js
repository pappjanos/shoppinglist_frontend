/*
export function dateFilter(value) {
  return value.substr(0, 10);
}

export function toUpperCase(value) {
  return value.toUpperCase();
}
*/

export default {
  dateFilter: (value) => value.substr(0, 10),
  toUpperCase: (value) => value.toUpperCase(),
};
